data:
  raw_dir: data/raw/diversevul
  processed_dir: data/processed
  max_seq_len: 500
  vocab_size: 10000
  vuln_feature_dim: 32
  train_ratio: 0.70
  val_ratio: 0.15
  test_ratio: 0.15
  augment_with_synthetic: true
  synthetic_vuln_repeat: 20
  synthetic_safe_repeat: 5

model:
  embed_dim: 256
  hidden_size: 256
  num_layers: 2
  dropout: 0.3
  attention_dim: 64
  cnn_num_filters: 128
  cnn_kernel_sizes: [3, 5, 7]
  num_attention_heads: 4

training:
  batch_size: 32
  lr: 0.001
  epochs: 20
  patience: 4
  grad_clip: 1.0
  seed: 42
  device: cuda
  use_focal_loss: true
  focal_alpha: 0.5
  focal_gamma: 2.0
  use_warmup_cosine: true
  oversample_factor: 2.5
  use_balanced_batch_sampler: true
  use_asymmetric_loss: true
  asym_gamma_neg: 4.0
  asym_gamma_pos: 0.0
  asym_clip: 0.05

evaluation:
  threshold_objective: f2
  threshold_beta: 2.0
  threshold_min_recall: 0.35
  threshold_start: 0.05
  threshold_end: 0.90
  threshold_step: 0.01
  ensemble_strategy: quorum
  ensemble_quorum: 3

results_dir: results/
